; ST7789 Display Test - Standalone PlatformIO Project
; Run: cd st7789_test && pio run --target upload

[env:esp32-c3-devkitm-1]
platform = espressif32
board = esp32-c3-devkitm-1
framework = arduino

monitor_speed = 115200
monitor_filters = direct

upload_protocol = esptool
upload_port = /dev/cu.usbmodem3101
monitor_port = /dev/cu.usbmodem3101

board_build.flash_mode = dio
board_build.f_flash = 40000000L

lib_deps =
    adafruit/Adafruit GFX Library @ ^1.11.9
    adafruit/Adafruit ST7735 and ST7789 Library @ ^1.10.4

; No special build flags needed - ESP32-C3 DevKit uses built-in USB-serial-JTAG
; which connects to UART0 automatically
build_flags =
